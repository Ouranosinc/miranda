============
Installation
============

At the command line either via pip::

    $ pip install miranda

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv miranda
    $ pip install miranda
